{
  "name": "job-triage",
  "version": "0.1.0",
  "description": "Local-first browser extension for triaging job listings",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "BUILD_TARGET=content vite build && BUILD_TARGET=background vite build && BUILD_TARGET=options vite build && mv dist/src/options/index.html dist/options/index.html && rm -rf dist/src",
    "build:content": "BUILD_TARGET=content vite build",
    "build:background": "BUILD_TARGET=background vite build",
    "build:options": "BUILD_TARGET=options vite build && mv dist/src/options/index.html dist/options/index.html && rm -rf dist/src",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "browser-extension",
    "jobs",
    "triage",
    "local-first"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "@vitest/coverage-v8": "^3.2.4",
    "fake-indexeddb": "^6.2.3",
    "happy-dom": "^20.0.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.10",
    "vite-plugin-static-copy": "^3.1.3",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "linkedom": "^0.18.12"
  }
}
